#!/bin/bash
if [ "$1" = "-g" ] && [ "$2" = "batt" ]; then
  printf 'Now drawing from '"'"'AC Power'"'"'\n -InternalBattery-0 (id=12345678)\t100%%; charged; 0:00 remaining present: true\n'
  exit 0
fi
printf 'Usage: pmset [-a|-b|-c|-u] <setting> <value>\n' >&2
exit 1
